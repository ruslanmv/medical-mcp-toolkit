# ----------------------------------------------------------------------
# Environment Variables for Medical MCP Toolkit
# ----------------------------------------------------------------------
# Copy this file to `.env` and fill in your values. The application
# (FastAPI HTTP shim + MCP server) will load them via python-dotenv.
#
# Tip (generate a strong token):
#   openssl rand -hex 32
# ----------------------------------------------------------------------

# --- Application Security ---
# Secret bearer token for authenticating HTTP API requests (/schema, /tools, /invoke).
# If unset, auth is disabled (dev mode). In production, ALWAYS set this to a strong secret.
BEARER_TOKEN=dev-token

# --- Database Connection ---
# PostgreSQL connection string used by adapters/services that need DB access.
# Default matches the provided Docker Makefile/db container.
DATABASE_URL=postgresql://mcp_user:mcp_password@localhost:5432/medical_db

# Optional: container bootstrap variables (only used by Dockerized Postgres).
# These are provided for convenience if you prefer `docker run --env-file .env`.
POSTGRES_USER=mcp_user
POSTGRES_PASSWORD=mcp_password
POSTGRES_DB=medical_db
DB_CONTAINER_NAME=medical-db-container
DB_PORT=5432

# --- Logging ---
# Log levels: DEBUG | INFO | WARNING | ERROR | CRITICAL
MCP_LOG_LEVEL=INFO
UVICORN_LOG_LEVEL=info

# --- (Optional) Network / Server tweaks ---
# These are not required for local dev; override if you template systemd/Docker.
# HTTP_HOST=0.0.0.0
# HTTP_PORT=9090
